type Query {
  users: [User!]!
}

type User {
  id: ID!
  email: String!
  password: String!
}

type Mutation {
  signup(email: String!, password: String!): AuthPayload
}

type AuthPayload {
  token: String
  user: User
}
